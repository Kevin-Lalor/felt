{
  "name": "@poker/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:leak": "cross-env PROPERTY=1 vitest run --testNamePattern=leak",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/static": "^8.0.1",
    "@fastify/websocket": "^11.0.1",
    "@poker/engine": "workspace:*",
    "@poker/protocol": "workspace:*",
    "fastify": "^5.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@types/ws": "^8.5.12",
    "cross-env": "^7.0.3",
    "tsx": "^4.19.1",
    "vitest": "^2.1.2"
  }
}
