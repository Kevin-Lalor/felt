{
  "name": "@poker/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:property": "cross-env PROPERTY=1 vitest run --testNamePattern=property",
    "typecheck": "tsc --noEmit"
  },
  "$comment": "dependencies MUST stay empty. See CLAUDE.md hard rule 5.",
  "devDependencies": {
    "@types/node": "^22.7.5",
    "@vitest/coverage-v8": "^2.1.2",
    "cross-env": "^7.0.3",
    "fast-check": "^3.22.0",
    "pokersolver": "^2.1.4",
    "vitest": "^2.1.2"
  }
}
