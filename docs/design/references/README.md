Visual references that are NOT specs — screenshots of other poker apps, mood boards, colour
inspiration, animation references.

Keep them separate from `../wireframes/` so there is never ambiguity about what Claude should
build from. Nothing in this folder is ever built from directly.
